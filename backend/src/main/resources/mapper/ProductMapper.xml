<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sqlcertification.demo.mapper.ProductMapper">

    <sql id="productFilter">
        <where>
            <if test="categoryName != null and categoryName != ''">
                AND cat.categoryname = #{categoryName}
            </if>
            <if test="keyword != null and keyword != ''">
                AND p.productname ILIKE '%' || #{keyword} || '%'
            </if>
            <if test="inStockOnly == true">
                AND p.stockquantity > 0
                AND p.isdiscontinued = FALSE
            </if>
        </where>
    </sql>

    <select id="selectProducts" resultType="Product">
        SELECT
            p.productid AS productId,
            p.productname AS productName,
            cat.categoryname AS categoryName,
            b.brandname AS brandName,
            p.unitprice AS unitPrice,
            p.stockquantity AS stockQuantity,
            p.isdiscontinued AS isDiscontinued,
            COALESCE(rv.avg_rating, 0) AS averageRating,
            COALESCE(rv.review_count, 0) AS reviewCount
        FROM products p
        JOIN categories cat ON cat.categoryid = p.categoryid
        LEFT JOIN brands b ON b.brandid = p.brandid
        LEFT JOIN (
            SELECT
                productid,
                AVG(rating)::NUMERIC(3, 2) AS avg_rating,
                COUNT(*) AS review_count
            FROM productReviews
            GROUP BY productid
        ) rv ON rv.productid = p.productid
        <include refid="productFilter"/>
        ORDER BY p.productid
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countProducts" resultType="long">
        SELECT COUNT(*)
        FROM products p
        JOIN categories cat ON cat.categoryid = p.categoryid
        <include refid="productFilter"/>
    </select>

</mapper>
