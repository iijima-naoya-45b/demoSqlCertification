<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sqlcertification.demo.mapper.SandboxAuditMapper">

    <insert id="insertAuditLog">
        INSERT INTO auditLogs (tableName, recordId, actionType, newValues)
        VALUES (
            'sql_sandbox',
            NULL,
            #{actionType},
            CAST(#{detailJson} AS jsonb)
        )
    </insert>

</mapper>
